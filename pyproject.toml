[project]
name = "virtnbdbackup-logparser"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "requests",
    "python-dotenv",
    "pytest>=9.0.3",
    "tzdata; sys_platform == 'win32'",
]

[dependency-groups]
dev = [
    "pytest",
    "pytest-mock",
]
